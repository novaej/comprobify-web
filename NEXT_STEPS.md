# Next Steps

Ordered backlog for `comprobify-web`. Items are numbered — complete the highest-priority items first. Remove an item when it ships; renumber the rest.

---

1. **Triage the 24 `npm audit` findings in production dependencies (3 critical, 11 high)**, surfaced by the new `.github/workflows/ci.yml` — including critical-severity findings in `next` itself, `next-auth`, and `@auth/core`. `next` is pinned to an exact version (`16.2.4`) with a non-breaking fix available (`16.3.5`); a plain `npm audit fix` pulls in a large, unrelated dependency churn and should not be run blindly — see `docs/production-readiness-checklist.md`'s "Security & CI hardening" section for what was already checked. Once triaged (fixed or explicitly accepted), flip the CI audit step from `continue-on-error: true` to blocking.
2. **Decide whether `comprobify-web` should go public, like `comprobify` did** (or find another way to add a required-reviewer rule to `staging-infra`/`production-infra`) — `staging-infra` currently has **zero** GitHub Environment protection rules, live-verified, meaning `terraform.yml`'s `apply-staging` job can run `terraform apply -auto-approve` against real infrastructure with no human approval gate. The API repo hit a billing-plan error adding this rule via `gh api` despite GitHub Team, and resolved it by making the repo public (required-reviewer rules are unrestricted on public repos). This is an account/repo-visibility decision, not something to default into — see `docs/production-readiness-checklist.md`'s "Security & CI hardening" section.
3. **Re-verify `extractForwardedIp()` (`src/lib/client-forwarding.ts`) against Caddy's actual forwarded headers before ever setting `INTERNAL_SERVICE_SECRET`.** It was written assuming `X-Forwarded-For`'s leftmost entry is the real client, under the old App Platform hosting — never empirically confirmed even then. Now Caddy sits in front (`deploy/caddy/Caddyfile`) and forwards the real client IP as `X-Real-Client-IP`, the same convention the Comprobify API's own Caddy-fronted droplet uses. `INTERNAL_SERVICE_SECRET` is already set on staging today (required for account-lifecycle and billing calls, see CLAUDE.md), so this is about the optional IP-override feature staying inert, not blocking anything load-bearing — see CLAUDE.md's "Forwarding the real visitor IP" entry.
4. **Actually apply production infrastructure.** `terraform/environments/production` and `.github/workflows/deploy-production.yml` now exist (mirroring staging exactly) but have never been run — no SSH key pair, GitHub Environments, or `terraform apply` yet. See `docs/production-readiness-checklist.md` for the full, actively-maintained punch list (this item used to duplicate that list here; now it just points at it, since the checklist is the one that stays current). Generate a **separate, dedicated** SSH key pair for it — do not reuse the staging droplet's key.
5. **A holistic application-code security pass hasn't been done for this repo** — tenant-isolation code (the `tenantId`-scoped Prisma query pattern), any markdown/HTML rendering, and permission-check call sites. The Comprobify API repo ran one for its own code (`../comprobify/docs/security-audit-2026-09-12.md`); this repo's equivalent hasn't happened yet and shouldn't be assumed clean by analogy — see `docs/production-readiness-checklist.md`'s "Security & CI hardening" section.
6. **Re-triage the Trivy image scan's baseline** (`node:24-slim`/`caddy:2-alpine`, now digest-pinned and scanned in both deploy workflows, informational only for now) once a real scan result exists from an actual workflow run, and decide whether to flip `exit-code` to `'1'` or add a `.trivyignore` — mirrors the API repo's own triage of its equivalent baseline.
7. **Consider `output: "standalone"` in `next.config.ts`** for a leaner Docker image once the App Platform → droplet migration is proven stable. The current `Dockerfile` deliberately copies the full `node_modules` through to minimize new failure surface on the first pass — see `docs/terraform-digitalocean-setup.md`'s "The Dockerfile" section.
8. **Consider wiring `/api/health` into an actual Docker healthcheck** on the `web` service in `deploy/docker-compose.yml`. Nothing currently probes it since the droplet migration — `restart: unless-stopped` is the only container-recovery mechanism today.
9. **Consider BuildKit secret mounts (`--mount=type=secret`) for `DATABASE_URL`/`SENTRY_AUTH_TOKEN` in `Dockerfile`** instead of plain `ARG`/`ENV`. Docker itself flags both with `SecretsUsedInArgOrEnv` warnings during `deploy-staging.yml`'s build — the actual exposure is limited (only the ephemeral `builder` stage on a GitHub Actions runner sees them; the pushed `runner` stage image doesn't inherit that layer history), but a secret mount would close the gap properly rather than relying on that multi-stage boundary.
