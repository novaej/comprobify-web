# Next Steps

Ordered backlog for `comprobify-web`. Items are numbered — complete the highest-priority items first. Remove an item when it ships; renumber the rest.

---

1. **Re-verify `extractForwardedIp()` (`src/lib/client-forwarding.ts`) against Caddy's actual forwarded headers before relying on it.** It was written assuming `X-Forwarded-For`'s leftmost entry is the real client, under the old App Platform hosting — never empirically confirmed even then. Now Caddy sits in front (`deploy/caddy/Caddyfile`) and forwards the real client IP as `X-Real-Client-IP`, the same convention the Comprobify API's own Caddy-fronted droplet uses. `INTERNAL_SERVICE_SECRET` itself is already set and working on both staging and production (confirmed via CLAUDE.md's ADR-035 notes and the checklist's staging-billing verification), so this is about the optional IP-override feature (`registrationLimiter`/`tenant_agreements.ip` resolving the real visitor instead of the droplet's shared egress IP) staying inert, not blocking anything load-bearing — see CLAUDE.md's "Forwarding the real visitor IP" entry.
2. **Consider `output: "standalone"` in `next.config.ts`** for a leaner Docker image now that the App Platform → droplet migration and the first production deploy have both proven stable. The current `Dockerfile` deliberately copies the full `node_modules` through to minimize new failure surface on the first pass — see `docs/terraform-digitalocean-setup.md`'s "The Dockerfile" section. Note this would also naturally resolve the `prisma`-CLI-in-the-runtime-image situation noted in `docs/production-readiness-checklist.md`'s npm audit triage (a leaner standalone image wouldn't need to ship the `prisma` devDependency into the runtime container at all).
3. **Consider wiring `/api/health` into an actual Docker healthcheck** on the `web` service in `deploy/docker-compose.yml`. Nothing currently probes it since the droplet migration — `restart: unless-stopped` is the only container-recovery mechanism today.
4. **Consider BuildKit secret mounts (`--mount=type=secret`) for `DATABASE_URL`/`SENTRY_AUTH_TOKEN` in `Dockerfile`** instead of plain `ARG`/`ENV`. Docker itself flags both with `SecretsUsedInArgOrEnv` warnings during `deploy-staging.yml`'s build — the actual exposure is limited (only the ephemeral `builder` stage on a GitHub Actions runner sees them; the pushed `runner` stage image doesn't inherit that layer history), but a secret mount would close the gap properly rather than relying on that multi-stage boundary.
5. **Rehearse the shared-database-cluster restore cutover.** A real DO-native test restore has been performed (new cluster boots, both databases' data confirmed intact via a direct DB client connection), but the actual cutover — pointing `DATABASE_URL` (this app's and/or the API's) at the restored cluster's connection string and confirming both apps keep working — hasn't been rehearsed yet. See `docs/production-readiness-checklist.md`'s "Backups" section for full context.
6. **Trigger a real test error in production and confirm it appears in the Sentry dashboard tagged `production`.** `SENTRY_DSN`/`NEXT_PUBLIC_SENTRY_DSN`/`SENTRY_AUTH_TOKEN` are all confirmed correctly set in the `production` GitHub Environment, but nobody has confirmed capture actually works end-to-end (e.g. the `org` slug in `next.config.ts`'s `withSentryConfig()` could still be wrong, source maps could fail to upload).

---

**Shipped this session (2026-09-15), removed from the numbered list above:** npm audit triage (24→4 findings, 0 critical — see the checklist's "Security & CI hardening" section for the 4 residual, accepted-as-low-risk findings); making the repo public + adding required-reviewer rules to `staging-infra`/`production-infra`; applying production infrastructure end-to-end (SSH keys, GitHub Environments, `terraform apply`, first production deploy); a holistic application-code security pass (tenant-isolation, XSS, the three-layer permission pattern — three real findings, all fixed); triaging the Trivy image scan baseline against real pinned digests (`docs/trivy-baseline-2026-09-15.md`).
