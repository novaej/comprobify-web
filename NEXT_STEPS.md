# Next Steps

Ordered backlog for `comprobify-web`. Items are numbered — complete the highest-priority items first. Remove an item when it ships; renumber the rest.

---

1. **Consider `output: "standalone"` in `next.config.ts`** for a leaner Docker image now that the App Platform → droplet migration and the first production deploy have both proven stable. The current `Dockerfile` deliberately copies the full `node_modules` through to minimize new failure surface on the first pass — see `docs/terraform-digitalocean-setup.md`'s "The Dockerfile" section. **Correction to an earlier note here:** this does *not* naturally resolve the `prisma`-CLI-in-the-runtime-image situation from `docs/production-readiness-checklist.md`'s npm audit triage — `start:deploy` runs `prisma migrate deploy && next start` inside the running container, so the `prisma` CLI still has to be present at runtime either way. Standalone's dependency tracer only follows what the app code actually imports (`@prisma/client`), not a CLI invoked via an npm script, so switching to standalone would likely need an explicit extra copy step for `node_modules/prisma` and its engines to avoid breaking migrations at startup — a real Prisma-specific standalone gotcha to plan for, not a side benefit that falls out for free.
2. **Consider BuildKit secret mounts (`--mount=type=secret`) for `DATABASE_URL`/`SENTRY_AUTH_TOKEN` in `Dockerfile`** instead of plain `ARG`/`ENV`. Docker itself flags both with `SecretsUsedInArgOrEnv` warnings during `deploy-staging.yml`'s build — the actual exposure is limited (only the ephemeral `builder` stage on a GitHub Actions runner sees them; the pushed `runner` stage image doesn't inherit that layer history), but a secret mount would close the gap properly rather than relying on that multi-stage boundary.

---

**Shipped this session (2026-09-15), removed from the numbered list above:** npm audit triage (24→4 findings, 0 critical — see the checklist's "Security & CI hardening" section for the 4 residual, accepted-as-low-risk findings); making the repo public + adding required-reviewer rules to `staging-infra`/`production-infra`; applying production infrastructure end-to-end (SSH keys, GitHub Environments, `terraform apply`, first production deploy); a holistic application-code security pass (tenant-isolation, XSS, the three-layer permission pattern — three real findings, all fixed); triaging the Trivy image scan baseline against real pinned digests (`docs/trivy-baseline-2026-09-15.md`); rehearsing the shared-database-cluster restore cutover; confirming Sentry production capture works end-to-end (via a real incident's errors appearing in the dashboard tagged `production`) — both now closed out in `docs/production-readiness-checklist.md`; fixing `extractForwardedIp()` to read `X-Real-Client-IP` instead of `X-Forwarded-For`, matching what Caddy actually sends and what the API repo's own identically-configured setup already reads; wiring `/api/health` into a real Docker healthcheck on `web` (verified end-to-end against a live container) and gating `caddy`'s startup on it.
